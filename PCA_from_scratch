import numpy as np

def generate_sample_data():
    rows = int(input("Number of rows: "))
    cols = int(input("Number of columns: "))

    data = np.random.rand(rows, cols) * 10
    print(data)


"""
FUNCTION generate_sample_data_v2
    INPUT number of rows → rows
    INPUT number of columns → cols

    CREATE empty matrix 'data' of size rows x cols

    FOR each column i from 1 to cols DO
        PRINT "Column i:"
        INPUT distribution type for column i → dist  (e.g., normal or uniform)
        INPUT mean for column i → mean

        IF dist is "normal" THEN
            INPUT standard deviation → std
            FILL column i of 'data' with random numbers from normal distribution with mean and std
        ELSE IF dist is "uniform" THEN
            INPUT range → range_val
            FILL column i of 'data' with random numbers from uniform distribution from (mean - range_val) to (mean + range_val)
        ELSE
            PRINT "Invalid distribution, using default normal distribution"
            FILL column i of 'data' with standard normal numbers centered at mean

    END FOR

    PRINT generated matrix 'data'
    RETURN 'data'
"""


